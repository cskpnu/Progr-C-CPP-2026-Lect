#include <iostream>

void calculate() {
    int x = 10; // Локальна змінна
    if (x > 0) {
        int y = 5; // Теж локальна, але її область ще вужча!
        std::cout << x + y; // Можна: 'x' видно у вкладеному блоці
    }
    // std::cout << y; // ПОМИЛКА! 'y' тут вже невидима (і знищена)
}
