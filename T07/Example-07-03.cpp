// СУЧАСНИЙ С++:
auto a = 5;       // Компілятор сам зробить a типу int
auto b = 3.14;    // Компілятор зробить b типу double
auto c = "Hello"; // Компілятор зробить c типу const char*

void myFunction() {
    auto int x = 10; // Явно вказуємо, що це автоматична локальна змінна
    int y = 20;      // Робить абсолютно те саме (auto мається на увазі автоматично)
}
