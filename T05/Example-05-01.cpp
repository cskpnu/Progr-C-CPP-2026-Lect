#include <string>

struct Student {
    std::string name; // поле: ім'я (текст)
    int age;          // поле: вік (ціле число)
    double gpa;       // поле: середній бал (дробове число)
};

Student st1; // Створили студента st1. Його поля містять "сміття" або порожні значення.
Student st2 = {"Іваненко Іван", 19, 4.5};
Student st3 {"Петренко Анна", 20, 5.0};
Student st4 {"Сидоренко Олег"}; // age буде 0, gpa буде 0.0

